#pragma once

//AVL
namespace BalanceBinaryTree
{
	// balanve factor: BF(Tree)= L_high - R_high;

	// blance binary tree should satisfy the abs(BF)<=1
	// give the n nodes for a AVL 
	// the maximum height = O(LOG2n)



}